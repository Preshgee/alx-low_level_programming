#include "main.h"
#include <stdio.h>

/**
 * main - prinys program name
 * @argc - number of arguments
 * @argv - arrays of arguments
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
